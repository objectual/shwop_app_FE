// @flow
import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  backgroundVideo: {
    position: 'absolute',
    top: 0,
    left: 0,
    bottom: 20,
    right: 0,
    zIndex: -1,
  },
});
